//displayMovieInfo function re-renders the HTML to display the appropriate content
function displayGifInfo() {
  var name = $(this).attr("data-name");
  var queryURL = "https://api.giphy.com/v1/gifs/search?q=" +
    name + "&api_key=dc6zaTOxFJmzC&limit=10&rating=PG-13";
    console.log(name);
    //create AJAX call for names and GIFs
    $.ajax({
      url: queryURL,
      method: "GET"
    }).then(function(response){
      console.log(JSON.stringify(response));
      console.log(response);
      var results = response.data;
      console.log(results);
      //loop through each result item
      for (var i = 0; i < results.length; i++);
      //create and store a div tag to hold the gif
      var gifDiv = $("<div>");
      //retrieve URL for image and create a variable for it
      var imgURL = results[i].images.fixed_height.url;
      // create an element to hold and store the image
      var image = $("<img>").attr("src", imgURL);
    })

}